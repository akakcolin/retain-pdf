# Rust 化实施状态

> 记录时间：2026-08-28。基于 git 历史 + 代码勘察（含未提交的工作区改动）。
> 进度远超 [13-Rust化实施计划.md](./13-Rust化实施计划.md)（该文档只写到 Phase 5）。

## 阶段进度

| Phase | 计划内容 | 实际状态 | 提交 |
|---|---|---|---|
| 0 | Tauri v2 替换 Electron | 完成 | 2849236a |
| 1 | 纯逻辑模块（layout/fit/payload/route） | 完成 | 15991635 |
| 2 | 差分测试（golden replay） | 完成 | 908d5604 |
| 3 | profile 数据形状层 | 完成 | 5aca7fdf |
| 4 | mupdf-rs reader + golden replay | 完成 | 0d57d9db |
| 5 | 5A 逻辑 + 5B 渲染链 + 5C 写路径 | 完成 | f1095aca |
| 6-8 | Typst 输出层 + source/background + pyo3 接入 | 完成 | 10988cca |
| 7R-1..6 | background/redaction 完整移植 | 完成 | cc1bfef0..0bfef7d6 |
| 7R-7 | 移除 page_specs/visual_profile 门禁 | 完成 | b88e5e7d |
| B2 | save_optimized 字节压缩接 native（fitz 子集化 + native garbage=4/流压缩，失败回退） | 完成 | a74d9e84 |
| B2-Inc2 | pdf_structure_profile 表单 xobject/几何/文本 span 接 reader 原语 | 完成 | e768013e |
| B2-Inc3 | reader PageSnapshot 补全（image_rects 聚合 + form_xobjects 原语） | 完成 | e768013e |
| B2-Inc4 | 分析簇接线：`build_render_document_analysis` bridge → `analysis/document/builder.py` | 完成 | e768013e |
| B2-Inc5 | 热路径清扫：整本 overlay 默认路径零 fitz 调用 | 完成 | e768013e |
| B2-Inc6 | save_optimized 子集化接 native：C shim `subset_and_clean`（隔离 context 跑 mupdf `pdf_subset_fonts` + garbage=4/流压缩，异常转 `mupdf_error_t**`），bridge 直调 `subset_and_save_optimized_pdf`，生产 `doc.tobytes()` 直传 native，fitz subset_fonts 移入回退 | 完成 | e2a7e9e7 |
| D2 | rendering crates 差分 + 冒烟 parity 接入 CI 门禁（rendering-parity.yml） | 完成 | 6f9e35d9..c2ad4e4a |
| C1 | Rust 编排器骨架 `rendering_orchestrator`（`render_rs --spec`）：prepare/page_specs 委托 `run_render_delegate.py` 产出 bundle，background→typst→save 全 native；`RETAINPDF_RENDER_ORCHESTRATOR_RS=1` test-gated 切换（未接生产配置）；`orchestrator_parity` 双二进制差分接 CI | 完成 | 092bc885 |
| C1·Ext | render_rs 接管 overlay/dual：delegate 按 mode 产出 `overlay_page_specs`（原 source 页几何 + RenderBlock DTO）与 `start_page`/`end_page`；新增 `stages/overlay.rs`（`run_overlay_compile`：emit overlay source + 无 `--root` 编译 + `merge_overlay_onto_base` 复用 `show_pdf_page`）与 `stages/dual.rs`（`build_dual_doc_pages` 共享 writer 原语合成双栏）；`orchestrator_parity` 扩到 typst/typst_visual/overlay/dual 四模式全等，`smoke_zero_fitz` delegate 探针扩到四模式 | 完成 | 2a75b532 |
| C3 | 生产接线 + 桌面打包：rust_api 按 mode 默认路由 typst/typst_visual→`render_rs`（auto/overlay/dual 仍走 python3），env 逃逸阀 `RETAINPDF_RENDER_ORCHESTRATOR_OFF/RS`；spawn 时给 delegate 透传 `RETAIN_PDF_PYTHON_BIN`/`RETAIN_PDF_RENDER_DELEGATE_SCRIPT`；`prepare-app.mjs` 打包 `bin/render_rs` + `build:render-rs` + release-desktop.yml 每平台构建 | 完成 | ccd4f5f3 |
| C3·Ext | overlay/dual/auto 全部默认路由到 `render_rs`：`should_route_render_rs` 匹配 `typst/typst_visual/overlay/dual/auto` 五模式（`auto` 由 delegate 解析为具体模式），env 逃逸阀 `RETAINPDF_RENDER_ORCHESTRATOR_RS=1` 强制 native、`RETAINPDF_RENDER_ORCHESTRATOR_OFF=1` 强制 python；worker_command 测试同步（overlay 路由 render_rs、console 模式走 translate 命令） | 完成 | 2a75b532 |
| C2-C3·Z | 终态判据 1 门禁：`smoke_zero_fitz` 差分桥——delegate `build_bundle`（typst/typst_visual）、auto render-mode 采样（`resolve_effective_render_mode`/editable/pseudo-scan）、整本 overlay 默认路径（`build_book_typst_pdf`→pikepdf 合并）三组探针断言 fitz 调用计数=0，且各断言 native 桥命中（非空过，全 python 回退时 delegate 会 +34 调用）；`build_dual_book_pdf` 增字节级 native 路径（pikepdf overlay 直出临时 PDF，双栏合并/TOC/字节压缩全走 bridge，`fitz.open` 归零，任一原语不可用回退原 fitz 文档流），`smoke_zero_fitz` 增 dual 探针，四种默认模式 + auto 采样全零 fitz | 完成 | f992ff2b/08a9643c |
| D1 | 双实现清零治理：`_routing.py` 增 `ALLOWLIST`（34 项，reason=`parity_reference`/`hard_boundary`）+ `NativeMandatoryError`；`routed()` 在 native 不可用时对未 allowlist fn 抛错（默认 ON，`RETAIN_PDF_NATIVE_MANDATE=0` 关），IN_MEMORY_PAGE 能力边界与 NATIVE_BRIDGE_ERROR 恢复不受限；新增 `smoke_d1_mandate` 差分桥（双向扫描：无未登记 routed fn、无 stale 条目；运行时验证 raise/放行/off-valve）接 CI | 完成 | c3e5d22d |
| D3 | 边界契约化：`contracts/bridge_shapes.py` 为单一 key 声明源（Python TypedDict + `shape_to_schema`）；`gen_contract_corpus.py` 跑真实生产 `build_bundle`/`_page_spec_to_dict`/`render_page_spec_to_bridge` 产出 6 边界 corpus（emitter/redaction page_specs、translated_pages、fill_map、precleaned_page_indices、render_bundle），`assert_keys_match` 先验；`rendering_orchestrator/tests/contract_strict_diff.rs` strict deny_unknown_fields 复刻 + 宽松 DTO 双解码；`test_bridge_contract.py` pytest 单测；CI 常驻（orchestrator cargo test + corpus 再生成 diff） | 完成 | e4303e45 |
| D4 | 统一路由与可观测：`routed()` 回退原因区分 feature flag 强制关闭（`FORCED_OFF`，`_fallback_reason_for_route`）与桥未导入（`NATIVE_NOT_BUILT`），原因日志精准化；`/health` 视图补 `native_fallbacks`（subsystem→reason→count）维度，`/metrics` 已暴露按子系统 hits/fallbacks-by-reason/hit_ratio（180655bc）；`test_routing.py` 增 FORCED_OFF 用例、rust_api health API 测试断言回退原因维度 | 完成 | e29700ce |
| D5 | 目标度量基线：`devtools/d5_baseline.py`（record/check）以 `devtools/d5_baseline.json`（schema `retainpdf_d5_baseline_v1`）登记四类目标基线——桌面体积（bundle-manifest `sizesBytes.total`，max 200MiB，锚点 4.1.10 darwin 实测 92.8MiB）、Python 渲染进程（`render_jobs_total{renderer=python}` 目标 0，C3 验收）、渲染耗时（`render_elapsed_seconds_sum/count` 均值，max 60s，锚点 15 页 overlay 实测 1.035s）、native 命中率（`native_hit_ratio{subsystem}`，min 0.9）；`record` 从 bundle-manifest + /metrics 刷新 observed，`check` 对照当前观测逐项判定（未观测 PENDING 不阻断，违规退出 1）；`test_d5_baseline.py` 29 用例 + `rendering-parity.yml` 自测步骤（committed 目标态 sample） | 完成 | e055f400 |
| C4 | 发布版打包 native bridge：release-desktop.yml 三平台 maturin build `rendering_bridge` wheel → `pip install --target` 装进 bundled python site-packages，组装期 import check 含 `rendering_bridge`；`prepare-app.mjs` `bundledRenderingBridgePresent`（兼容顶层 `.so/.pyd` 与 `rendering_bridge/` 包目录）写 `renderingBridgeBundled` manifest + validator 门禁；`rendering-parity.yml` 增 `bridge-bundle-smoke` 三 OS wheel 构建/加载冒烟 | 完成 | 58db7ba9 |
| B-F | `show_pdf_page`/`build_dual_doc_pages` 接 native：`rendering_writer::overlay::show_pdf_page` 纯 Rust 合成（双 Form XObject + `calc_matrix` 复刻 fitz 语义，含 rotated 页 transformation-matrix 覆盖）；bridge 导出两函数；`output/typst/_native.py` 增 `show_pdf_page_on_doc`/`build_dual_doc_pages` shim（native 返回新 doc、回退原地）；单页/dual-book/overlay_book 逐页接线 + doc_slot 换手；`page_overlay.overlay_pages_from_single_pdf` 主循环经 `compositor` 注入接 native（source 层不 import output/typst，由 `output/typst` wrapper 注入 `_native.show_pdf_page_on_doc`，doc_slot 换手 + legacy 计数仅真实 fallback 自增）；新增 `smoke_show_pdf_page_bridge` 接 CI | 完成 | ef2127b3 |
| B-F·TOC | `copy_toc`/`copy_toc_for_page_map` 接 native：`rendering_writer::background::toc` 增 `start_page`/`end_page` 范围 + page-map 变体（mupdf `set_outlines`，条目计数 0=未变更不换手）；bridge 导出两函数（bytes 进 bytes 出）；`document/metadata.py` 经 `document/_native.py` shim 路由（get_toc+set_toc 全 native，回退纯 fitz），返回 doc 换手契约（6 调用点：stage/book_support/book_renderer/dev_overlay 按 swap pattern 关旧 doc）；新增 `smoke_copy_toc_bridge` 接 CI | 完成 | ef2127b3 |
| B-G | 红批写入原语（破 `text_redaction`/`text_layer_only` 回退）：`text_layer_only` 四子路由（image_page/cover_only_count/vector_heavy/standard）executor 全量移植（`routes.rs` 不再 deferred 回退），`fill.rs` prepared cover 采样/绘制原语；`_native_eligible` 对全部策略放行（不再 STRATEGY_NOT_PORTED 回退，仅 instrumented stage 回退）；新增 `smoke_text_layer_only_bridge`（各子路由页 facts 全等）接 CI；`_routing.py` 删死枚举 `STRATEGY_NOT_PORTED` | 完成 | ce348261/044f697a |
| B-D | `visual_profile`/`color_adapt` 纯几何清理：`color_adapt.py` 的 `title_text_color_from_text_spans`/`title_text_color_from_visual_components`/`_sample_item_cover_fill`/`apply_adaptive_overlay_colors` 4 处 `fitz.Rect(...)` 构造改纯 `Rect`（几何判断），fitz 消费边界显式 `to_fitz()`（clip 与 fill.py sampler 传参）；`visual_profile/foreground.py` 的 `fitz.Rect(rect) & page.rect` 为必需 fitz 消费（get_pixmap clip），调用方均传 fitz.Rect，不属纯几何强转、保持不动；smoke_color_adapt_bridge + 45 相关单测通过 | 完成 | 8a4f198d |
| B-B | `source_cleanup/planning` 收尾 + 删 reference：删除仅被 smoke 使用的纯 fitz 参考实现——`planner.py` 的 `_plan_source_cleanup_python`/`_item_ids_with_uncovered_unsafe_vector_overlap_python`/`page_uncovered_unsafe_vector_item_ids`/`_plan_form_xobject_page`（生产走 `_plan_source_cleanup_from_contexts` ctx 路径 + `plan_source_cleanup_page_ctx` 的 form-xobject 分支）；`page_features.py` 的 `build_page_cleanup_features`（含 page_probe 引用）；`planning/rects.py` 的 `merge_rects` fitz 分支（纯 `Rect` 类型保持，死代码清除）。保留生产回退 `_build_context_from_fitz`/`_build_page_contexts_python` 与测试用的 fitz wrapper（`plan_source_cleanup_page` 等）；smoke_source_cleanup_planning 改 2-way parity（native vs NATIVE=False），389 渲染单测通过 | 完成 | 836a2e89 |
| C3-N1 | `source_cleanup/planning` 候选装配 Rust 化（~1750 行，19 模块）→ `rendering_core::source_cleanup::planning`：`plan_source_cleanup` + `item_ids_with_uncovered_unsafe_vector_overlap` 生产走 bridge（新增 `plan_source_cleanup_native`/`uncovered_unsafe_vector_item_ids_native`，bytes+JSON 进出），`_native.py`/`planner.py` 路由，NATIVE=False 回退 ctx 参考（`_plan_source_cleanup_from_contexts`/`_item_ids_with_uncovered_unsafe_vector_overlap_python`，B-B 删的纯 fitz reference 未回潮）；`_routing` allowlist 32→34（reason=`parity_reference`）；`rect.rs::round_to_digits` 精确复刻 CPython `round(x,n)`（mantissa 半偶整数舍入，520 值 fuzz 校验）；smoke_source_cleanup_planning 扩展 3-way context parity + candidates/uncovered 2-way + native-hit/fitz-zero（94 Rust 单测 + 419 渲染单测 + 冒烟全绿） | 完成 | ac802c9c |
| C3-N2 | layout seed 边界 `build_block_payloads` 接 native：`rendering_core::layout` seed 叶模块（block_seed_body_policy/body_context/fit_metrics/line_structure/render_item/render_text/title_binary_fit/typography_memory）+ `payload` 装配三件套（block_seed_metrics/block_seed_payload_factory/block_seed）+ geometry_adjustments；Item 模型扩展 `from_json_value`（反序列化 translated-item dict，含 `_render_*` 前导下划线键）+ text_common/tokens 补 render_text/plain_text/protected-token 原语；bridge 导出 `build_block_payloads`（typed Item + raw JSON 双输入，`title_fit` 按 JSON 对象出、shim 重建 `TitleFitDecision`）；`layout/payload/_native.py`+`blocks.py` 路由（NATIVE=False 回退 block_seed），`_routing` allowlist 34→35；smoke_build_block_payloads_bridge 接 CI（title_fit asdict/colors tuple 归一，边界空文本/缺 bbox 双侧一致；124 Rust 单测 + 419 渲染单测 + D1 门禁全绿） | 完成 | 79aa2842 |
| C3-N2·Em | layout emit 边界 `emit_render_blocks` 接 native：`rendering_core::payload` emit 装配（payload_to_render_block/emit_render_blocks——title_fit 分支直读 8 fit 字段，否则 `resolve_typst_binary_fit` 复刻 `{**item, "_render_inner_bbox": safe_inner_bbox, ...}` 预置覆写 + `_body_font_unified`/`_allow_short_text_bbox_overflow` 清零，RenderBlock DTO 全字段含 preserved_line_boxes/toc_entries/math_map）+ inline_content 链（placeholder_markdown/formula 归一/mitex 定界符/typst 保护文本 sanitize，`\w`/`\s` 字节语义 + char 边界防护）+ fit 叶模块（fit_common/fit_typst_context/fit_typst_bounds/formula_safety/policy_compat）+ toc_structure/toc_document + `line_structure.rs::preserved_line_boxes_for_item`（translated 行与 source lines bbox 逐行 zip，形状不匹配整体放弃）；bridge 导出 `emit_render_blocks`（raw JSON block_payloads 进出）；`layout/payload/_native.py::emit_render_blocks` shim 重建 RenderBlock dataclass（`_render_block_from_dict`/line_box/toc_entry）+ `blocks.py` 路由，`_routing` allowlist 35→36；smoke_emit_render_blocks_bridge 接 CI（build-flow 全等 + title_fit/fit-decision(11.14pt/0.35em/60pt)/preserve-line-break 三分支断言；190 Rust 单测 + 419 渲染单测 + D1 门禁全绿） | 完成 | b88c1cc0 |
| C3-N3 | layout body-pipeline 边界 `apply_body_pipeline`/`resolve_book_body_font_target` 接 native：`rendering_core::payload::body_pipeline` 编排器全量移植（BODY_PRE_GROW/POST_GROW/UNIFY 阶段，role_min 交错 unify×2 + 双 recover_underfilled_density + refit_leading_after_font_unify）复刻 `body_pipeline.py`；body 克隆 + body_indices 映射双向同步协议（CloneStage 改克隆回写 ordered，OrderedStage 读克隆锚点改 ordered 再同步回克隆）保持 `blocks.py` 共享 dict 对象同一性；`_native.py::apply_body_pipeline` shim 经 `payload.clear(); payload.update(updated)` 写回 + `title_fit` dataclass asdict/还原，`resolve_book_body_font_target_from_payloads` 剥 title_fit 过 JSON 边界；bridge 导出两函数；`blocks.py` 路由，`_routing` allowlist 36→38（`parity_reference`）；修 `recover_payload_density` 循环丢弃 font step 返回值致 off-mode 字体分歧（11.92 vs 12.76，改 `changed = font_step or changed`）+ `_anchor_score` 补 `source_text` 兜底与 strip；smoke_apply_body_pipeline_bridge 接 CI（body unify 收敛 10.0/page_body_font_size_pt 标注/title_fit 往返/caption-footnote annotation 收敛 9.68·9.02/book target 10.0 双侧全等；246 Rust 单测 + 419 渲染单测 + D1 门禁全绿） | 完成 | ed160427 |
| C3-N4 | layout 碰撞边界 `mark_adjacent_collision_risk` 接 native：`rendering_core::layout::collision::mark_adjacent_collision_risk`（collision_context 上下文 + fit_vertical 垂直重拟合，同 Python `collision.py`）；修 C3-N3 遗留 parity 缺口——Rust 端口 re-fit 分支缺 `adjacent_collision_risk=True` 标记（collision.py 有，emit 消费该键），补 `obj.insert` + 单测断言；bridge 导出 `mark_adjacent_collision_risk`（ordered payloads JSON 进出）；`_native.py` shim 经 `payload.clear(); payload.update(updated)` 写回 + `title_fit` dataclass asdict/还原，`blocks.py` 把 `mark_adjacent_collision_risk` 从纯 Python collision 导入改走 `_payload_native`，`_routing` allowlist 38→39（`parity_reference`）；smoke_collision_bridge 接 CI（re-fit 分支 `adjacent_collision_risk`/字体压缩/unified-leading 分支仅压 leading 0.56/roomy 不动/tight 非 body 也走 re-fit（碰撞循环无 is_body 门）/title_fit 往返，双侧全等；246 Rust 单测 + 419 渲染单测 + D1 门禁全绿） | 完成 | 89d8f8f5 |
| C3-N5 | layout seed 边界 `seed_render_fields` 接 native：`rendering_core::layout::render_item::seed_render_fields`（Value 级直改 raw item dict——Item 反序列化仅在 `should_skip_display_math_render`/`should_render_source_block` 判定按需，保未知键 byte-exact 往返）+ 文本流/行结构移植（`text_flow.rs` 全量复刻 `document_schema/text_flow.py`；`line_structure.rs` 增 preserve 机制 `maybe_preserve_structured_line_breaks`/`source_line_texts`/`looks_like_structured_line_block`/`split_text_by_source_line_markers`，无 regex 依赖改字节扫描 + CJK 全字符边界防半字节误判）；bridge 导出 `seed_render_fields`；`_native.py` shim 经 `payload.clear(); payload.update(updated)` 写回，`blocks.py` 的 `build_render_blocks` 首步由纯 Python for 循环改走 `_payload_native.seed_render_fields`，`_routing` allowlist 39→40（`parity_reference`）；smoke_seed_render_fields_bridge 接 CI（11 fixtures：普通段/same-meaningful 空白折叠清 render_protected/display-math 跳过清字段+source 回填/formula 保留与未译 source 回退/group 链/continuation 链/结构化单行拆分+`_render_preserve_line_breaks` 标记/caption 长行折叠/formula-map 链克隆与非列表置空，native vs NATIVE=False byte-exact 逐项相等；274 Rust 单测 + 419 渲染单测 + D1 门禁全绿）；差分验证揪出两处判定分歧并修复——(1) `block_kind` present-wins：native 经 `semantics::block_kind` 对 present-but-empty `block_kind=""`+`block_type="formula"` 落到 formula，Python reference 用内联 `item.get("block_kind", item.get("block_type","")) or ""` 得空串，`render_text.rs` 改走 `seed_block_kind`（present-wins 复刻，Some("") 即空，None 才落 block_type）；(2) 裸反斜杠判定：`should_render_source_block` 的 seed reference 是 `render_item.py` 的 `latex_command_count > 0` 而非 `render_text.py` 的 `"\\" in source_text`，改 `analysis.latex_command_count() > 0`（`\(x^2\)` 无 latex 命令不 source 回退）+ 5 回归单测 | 完成 | 038b28c9 |
| C3-N6 | layout page-specs 装配段接 native：`page_specs._layout_page_spec` 的 body-pipeline/碰撞/emit 三段由纯 Python 直调（`apply_body_payload_pipeline`+`unify_annotation_fonts`+`mark_adjacent_collision_risk`+`emit_render_blocks`）改走 `_payload_native`（镜像 `blocks.py` 共享步骤：`apply_body_pipeline`/`mark_adjacent_collision_risk`/`emit_render_blocks`），顺带把该路径统一到 C3-N3-blessed 行为——native `apply_body_pipeline` 内含 `recover_underfilled_annotation_density`，原 page_specs 路径缺失；`observe_payload_typography`（进程内 learning）与 `apply_title_fit_budget_to_render_blocks`（无 native 端口）刻意留 Python；`_routing` allowlist 保持 40（复用 C3-N2/N3/N4 登记，无新增 routed fn）；smoke_page_specs_bridge 扩展第 4 部分（build_render_page_specs native vs NATIVE=False byte-exact 逐页相等 + 每 stage native hit ≥12 + 零 layout_payload fallback，已接 CI）；delegate（`run_render_delegate.py`）与 `book_renderer.py`/`prewarm_page_specs.py` 的 page_specs 由此全走 native，bundle `page_specs` 喂 `stages/typst.rs`/`stages/background.rs` 消费；274 Rust 单测 + 419 渲染单测 + D1 门禁全绿 | 完成 | 5e39fa77 |
| C3-N7 | layout prepare 装配段接 native：`prepare.prepare_render_payloads_by_page`（`build_render_page_specs` 前最后一段纯 Python 装配）整体移植到 `rendering_core::payload::prepare`（`PrepareState` flat_items+page_ranges+page_metrics 复刻 Python 共享引用变异语义、结果自 page_ranges 重建；`_continuation_adjusted_capacities` 窄框容量松弛）+ `rendering_core::layout::suspicious_ocr`（粘连块剔除，item `render_diagnostics` 保留、summary 仅打印丢弃）；C3-N7a 叶支撑：`first_line_indent.rs` candidate 判定/`render_item.rs` group 助手（group_render_unit_items/protected/source/formula_map）/`block_seed_metrics::block_metrics`/`payload_dict`；bridge 导出 `prepare_render_payloads_by_page`（translated_pages + first_line_indent_lookup/effective_inner_bbox_lookup 两可选预计算 JSON，native 深拷贝不改调用方）；`_native.py` shim 用 `prepare._build_page_metrics`+`_resolve_first_line_indent_lookup` 把 `source_pdf_path` 解析成 indent lookup 再喂 native（Rust 叶 crate 打不开 PDF），prepare.py 拆薄路由 wrapper（体改名 `_prepare_render_payloads_by_page_python`），`_routing` allowlist 40→41（`parity_reference`）；smoke_prepare_render_payloads_bridge 接 CI（group 拆分拼接还原/continuation 成员免拆+单位拆分/同义清字段/direct-typst math 免剔/OCR 粘连块剔除/lookups 附着 + 调用方输入不可变 + page_specs 集成 native hit/零 fallback 全等；303 Rust 单测 + 419 渲染单测 + D1 门禁全绿） | 完成 | b977f90a |
| C3-N8 | layout policy-fields 边界接 native：`policy/cleanup_policy.py` 的 `apply_render_pages_policy_fields`/`apply_render_page_policy_fields`（`prepare_translated_pages_for_render` 的 `prepare_render_payloads_by_page` 之后一段，按 typst-fill/默认文字覆盖填充/no-op 三模式把 `_render_policy` 写回 item）移植到 `rendering_core::layout::policy_fields`（Value 级直改保未知键 byte-exact；`document_schema/semantics.block_kind`/`item_rect`/`item_render_output_text`/`item_is_marked_non_translated`（status/decision/tags）全移植；`use_typst_fill_cleanup`/`use_default_text_overlay_cover_fill` 两运行时配置旗标由 shim 在调用时从 `layout.apply_layout_tuning` 结果解析传入）；bridge 导出 `apply_render_pages_policy_fields`（translated_pages + 两旗标，页级变体经 `{0: items}` 包装/解包）；新增 `policy/_native.py` shim 路由 + `cleanup_policy.py` 拆薄路由 wrapper（体改名 `_apply_render_pages_policy_fields_python`/`_apply_render_page_policy_fields_python`），`_routing` 增 `policy` 子系统 + allowlist 41→43（`parity_reference`）；smoke_policy_fields_bridge 接 CI（三模式 byte-exact + 非译项/空 item_id 排除 + 单页变体 + `prepare_translated_pages_for_render` 集成 policy hit/零 fallback 全等；313 Rust 单测 + 419 渲染单测 + D1 门禁全绿） | 完成 | f398a82b |
| C3-N9 | source hidden-text strip 接线 native：`source/preparation/hidden_text_strip.py` 的 `build_hidden_text_stripped_pdf_copy` 保留 fitz 候选页预扫描（`page_is_pseudo_editable_scan`，硬边界），逐页 strip 委托给候选页过滤版 native——`rendering_writer::hidden_text` 增 `strip_hidden_text_pages`（整本 `strip_hidden_text` 保留为 write-diff 参考，共享 `strip_hidden_text_on_pages(doc, Option<&[i32]>)`：页索引越界/去重/空集忽略）；bridge 导出 `strip_hidden_text_pages`（bytes + 页索引 JSON 进出）；`source/_native.py` 增 `build_hidden_text_stripped_pdf_copy(candidate_pages, ...)` 路由 + `hidden_text_strip.py` 拆薄路由 wrapper（体改名 `_build_hidden_text_stripped_pdf_copy_python`），`_routing` allowlist 43→44（`parity_reference`）；smoke_hidden_text_strip_bridge 接 CI（corpus 复放 counts/页 facts/逐 token 内容流全等（Rust serialize 与 qpdf 数组空格差异非 byte-exact，按操作符+归一化操作数比较）+ 2 页 fixture 候选过滤（strip 页 0 页 1 不动、仅 strip 页 1 为 no-op）+ native hit + NATIVE=False 回退；write_diff 增 filtered-path 测试；314 Rust 单测 + 419 渲染单测 + 33 冒烟全绿） | 完成 | 25a624c7 |
| C3-N10 | 红批 executor bbox-text-strip 接线 native：`source_cleanup/pdf/document.py` 的 `strip_bbox_text_rects_from_pdf_copy` 拆薄路由 wrapper（体改名 `_strip_bbox_text_rects_from_pdf_copy_python`，parallel/sequential `_strip_pages`/`_apply_page_results`/`_save_pdf_atomic`/deadline 预算全保留），新增 `source_cleanup/pdf/_native.py` shim 经 `_routing` 路由——bridge `strip_bbox_text_rects` 升级返回 `(bytes, StripPdfResult JSON)`（`cleanup_writer.rs::StripPdfResult` 补 `serde::Serialize`，pages_changed/text_show_ops_removed/forms_changed/changed_page_indices 元数据直出）；`skip_form_xobject_pages=True`/`max_elapsed_seconds` 门禁回退 Python reference（native 引擎无逐页 form-skip 检测或 deadline 预算），`_routing` 增 `source_cleanup` 子系统 + allowlist 44→45（`parity_reference`）；smoke_source_cleanup_executor_bridge 接 CI（corpus 5 case 复放 counts/页 facts/逐 token 内容流全等（Rust serialize 与 qpdf 数组空格差异非 byte-exact，按操作符+归一化操作数比较）+ skip_form/deadline 路由门禁 0 native hit + native hit + NATIVE=False 回退）；314 Rust 单测 + 419 渲染单测 + 34 冒烟全绿 | 完成 | ffb1cecd |
| B-C | `source/cleanup` 文本读取补全：`text_intrusion.collect_page_intrusive_display_text_rects` / `margin_text_cleanup._candidate_margin_block_rects` 接 native spans/blocks 原语（`extract_page_text_spans`/`extract_page_text_blocks` → `source/_native.py` 经 `_routing` 路由，file-backed 页走 `read_page_text_spans`/`read_page_text_blocks`）；words-clip（`extract_item_word_entries` 的 `get_text("words", clip)`）留 reference（mupdf-rs 无等价物，硬边界 allowlist）；smoke_text_read_bridge 的 `check_intrusion_and_margin` 断言 native 桥命中 + 零 `page.get_text` + native==回退全等，接 CI | 完成 | 044f697a/b88e5e7d |
| B-E | `source` 顶层散点收编：`vector_profile.page_drawing_count` 接 native 计数原语（`read_page_drawing_count`，生产消费方 page_overlay/route_context 走 native）；`compression.analysis.source_pdf_has_vector_graphics` 经 `page_drawing_count` 全 native（阈值阶梯 + 缺失路径短路，file-backed 零 `get_drawings`）；`document_ops`（`page_word_count`/`_visible_text_traces`/`page_has_editable_text`/`page_is_pseudo_editable_scan`）留 reference（`get_texttrace`/words 硬边界，strip_hidden_text 预扫描语义）；`vector_profile.collect_page_drawing_rects` 留 reference（per-drawing zigzag rect quirk）；新增 `smoke_source_top_level_bridge` 接 CI | 完成 | b88e5e7d/044f697a |
| B-H | 纯几何 `Rect` 强转清扫（40+ 文件）：`source/cleanup` 文本读取+红批链全纯 `Rect`——`text_extract` 的 `_extract_page_text_blocks/spans_python`、`math_spans`、`source/_native.py` 反序列化（`_deserialize_text_entries`/`_deserialize_rects`）→ `Rect(*...)`；`redaction_padding` 四 expand、`text_rects`/`text_ownership`/`text_math_guard`/`text_safe_direct`/`margin_text_cleanup`/`standard`/`standard_execution`/`auto` 类型提示转纯；`math_intrusion` 修 `fitz & pure` 隐患（`coerce(item_rect) & protected`）；`background` 平铺检测链（`detect._image_rects`/`_tiled_images_covered`/`_merge_vertical_image_bands` 改不可变 `include_rect`、`patch.merge_close_vertical_rects`/`map_rect_to_image`、`image_route` 条目 bbox、`page_overlay` 红批 bbox）纯化，picker `pick_primary_background_image` 保持 fitz 契约（测试断言 + image_route 消费）；`vector_profile._rects_from_drawings`/`_expand_thin_drawing_rect` 纯化（fitz 零点半真值谓词 `not any(coords)` 保留）+ `_native.py` 签名；`pdf_structure_profile/sampler`（text_rects/_rect_from_bbox/_overlap_ratio）、`path_removal`（`coerce(path_rect)`）、`source/background/_native.py` is_empty/is_infinite 检查与 title-color `coerce`、`vector_text` 本地几何 helpers 纯化；保留必需 fitz 消费：words-clip、get_pixmap clip（visual_profile/foreground + layout `first_line_indent`——架构契约禁 layout→source.rects）、show_pdf_page、fill.py sampler、valid_items/item_rects、`collect_vector_text_rects`（返值供红批链）；389 渲染单测 + 8 差分 smoke 通过 | 完成 | c2b50964 |
| B-A | `render_mode.py` + `analysis/profile`+`classifier` 收编：render_mode auto 探针（`is_editable_pdf`/`is_pseudo_editable_scan_pdf`/`resolve_effective_render_mode`）全走 native `build_render_document_analysis`（044f697a，`smoke_render_mode_bridge` + `smoke_zero_fitz` auto 采样零 fitz）；per-page `classify_render_page` 接 native 薄函数（bridge `classify_render_page`：page_snapshot → `rendering_core::classifier`，复用整本文档分析 reader 原语，无新 reader 原语），`analysis/classifier.py` 增 `classify_render_page_pdf` routed 入口 + `_routing` allowlist `parity_reference`，新增 `smoke_classify_render_page_bridge`（golden+合成 parity、native 命中、零 fitz、NATIVE=False 回退）接 CI | 完成 | 044f697a/fd608bb7 |
| 终态判据4 | fitz import 收缩为可枚举 allowlist：`devtools/architecture_checks/fitz_imports.py` 新增 `check_fitz_import_allowlist`——非 devtools 模块 import fitz 必须登记 `FITZ_IMPORT_ALLOWLIST`（100 项，分类 `hard_boundary`/`fallback_reference`/`non_default_write`/`non_render_service`，每项带 reason），stale 条目（模块已停 import fitz）与未登记新 import 即失败，`devtools/` 整体豁免；清除 5 个死 import（`patch`/`color_sampling`/`text_rects`/`redaction_padding`/`path_removal`），fitz 面 104 → 100；接入 `pipeline.main()` 经 `test_pipeline_architecture_contract_passes`（main()==0）自动进 CI pytest，无需改 rendering-parity.yml | 完成 | 52172a77 |
| C3-N11a | delegate `build_bundle` 原生骨架：`bundle_builder/`（mod.rs + assemble.rs）native 组装 15-key `render.bundle.v1`（serde_json `preserve_order` 定序），`assemble.rs` 产出 schema_version/mode/source_pdf/output_pdf/work_dir/font_family/redaction_strategy/precleaned_page_indices/visual_profile_fill_map/page_map/translated_pages/page_specs/start_page/end_page/overlay_page_specs 15 键 dict；N11a 实键——mode/typst_font_family（RAW 值）/output_pdf（`<rendered>/<name>`，name=spec 值或 `{stem}-translated.pdf`）/work_dir（`<rendered>/typst/background-book`，rmtree 重建）/redaction_strategy（typst_visual→visual_cover else None）/visual_profile_fill_map={}/page_map=sorted(selected 页键)/start_page=max(0,spec)/end_page（stop_page：`end_page<0`→max(selected) else spec 值）/overlay_page_specs=None，source_pdf/precleaned=[]/translated_pages={}/page_specs=[] 为 N11c/N11d/N11f 占位；`translation.rs`（R-1）load/select_translated_pages 复用，`protected_pages.rs`（R-2）留 N11b；门禁 `RETAINPDF_RENDER_BUNDLE_NATIVE`（默认关，`0/false/off/no` 强制关）在 run.rs 选 native/delegate，`render_rs --dump-bundle <path>` 隐藏差分 hook（只出 bundle 不跑 stage），overlay/dual/auto 干净拒绝（N11e/N11b）；`orchestrator_bundle_parity.py` 差分 smoke（native dump vs delegate `--bundle-out` 子集键断言，float/路径归一化）+ CI 接线 | 完成 | c2958dde |
| C3-N11b | 整本文档分析 + auto 模式原生解析：`bundle_builder/analysis.rs`——`build_render_document_analysis` 开 PDF 一次遍历 selected 页（`[0,page_count)` 过滤，UFCS `PdfDocument::page_count` 避 mupdf inherent i32）page_snapshot→`build_render_page_profile`→`build_render_page_analysis`（`render_document_profile_v1`，`background_coverage_ratio` 6dp 舍入）；派生页集 port `contracts/document_analysis.py`（`pikepdf_text_strip_page_indices`/`visual_cover_page_indices`/`hidden_text_strip_page_indices` + `route_reason_counts`，BTreeMap 定序保前 3 页采样确定性）；`resolve_effective_render_mode` port `render_mode.py`（`is_pseudo_editable_scan_analysis`/`is_editable_analysis`，前 3 页 `editable_pages >= max(1, ceil(sampled/2))`，全 pseudo-scan 即 false）；build_bundle 的 auto 经原生分析解析为 typst_visual（非可编辑）或 overlay（可编辑，N11e 前 bail「auto resolution produced overlay」），`redaction_strategy` 随解析后 mode（auto→typst_visual→visual_cover）；`protected_pages_from_document_path` 加载接线（`translations_dir.parent/ocr/normalized/document.v1.json`，错 schema 即刻报错同 delegate，N11c 消费）；差分 smoke auto 移出 `_UNSUPPORTED_MODES` 按 typst_visual 键集断言（fixture 解析到 typst_visual，双 producer 同分析）；删 R-3 探索残留 `document_analysis.rs`/`tests/tmp_da_check.rs` | 完成 | dc33c42e |
| C3-N11c | render source 构建链接 native：`bundle_builder/render_source.rs`——`build_render_source_pdf` port `services/rendering/source/render_source.py` 四步链：① invalid-xobject sanitize（`sanitize_invalid_xobjects`）② hidden-text strip（候选集=`document_analysis.hidden_text_strip_page_indices` 伪扫描集，`strip_hidden_text_pages`，corpus 空跳过）③ bbox-text strip（`plan_source_cleanup` 候选 + `allows_pikepdf_strip` 映射 + `strip_bbox_text_rects_from_pdf`（recurse_forms=True），changed_page_indices 排序即 `precleaned_page_indices`）④ image compress（`pdf_compress_dpi>0`）；`work_root=typst_temp_root(output_pdf)`=`<rendered>/typst`，中间件 `intermediate_pdf_path`（`{stem}{suffix}` 优先 / sha256[:16] 兜底 / 12-hex 兜底，POSIX 240/1000 字节上限），`normalize_source_cleanup_strategy`/`use_bbox_text_strip_cleanup` port `layout`；build_bundle 实接 `source_pdf`=最终中间件路径 + `precleaned_page_indices`，`protected_pages_from_document_path` 提前加载（错 schema 即刻报错）；`build_planning_contexts` 从 bridge 挪至 `rendering_reader::cleanup_context`（行为中立，bridge + orchestrator 双消费）；差分 smoke `_ASSERTED_KEYS` 增 source_pdf + precleaned_page_indices（typst/typst_visual/auto，双 producer bbox-stripped 中间件全等 + precleaned=[0,1]） | 完成 | 1da6b6ba |
| C3-N11e | prepare 后 color-adapt + overlay/dual 装配接 native：`bundle_builder/color_adapt.rs`——`apply_adaptive_overlay_colors_batch` port `output/typst/_native.py`（四模式统一入口、precomputed 恒空：overlay/dual 经 `overlay_color.apply_overlay_page_colors`、typst 经 `book_renderer._apply_background_page_color_adapt`（visual profile 不加载，`merge_visual_profile_colors(None,None)` 直通）全汇到 `_apply_adaptive_overlay_colors_with_data` 决策树）；单 doc 开一次逐页 page_rect（不可读页 pass-through）→ batch 采样器+target fills（`build_local_background_sampler`/`sample_local_background_fill` scale 2.0）→ 整页/逐 title span dict（`build_text_page_for_extraction`/`extract_span_dicts_from_text_page`，title≥2 才整页）→ title-visual probes（`render_page_clip_rgb` scale 3.0 → `title_foreground_color_from_pixmap`）→ `adapt_item` 决策树写 `_render_cover_fill`/`_render_text_color`（needs-local-sampling 取 fill_by_item_id、title_like 才走 span/visual、`text_color_for_fill` 亮度≤0.42 白）；`bundle_builder/overlay.rs`——`build_overlay_page_specs` port delegate overlay/dual 段（`read_source_page_sizes`=RAW source `page_rect` 尺寸，越界/不可读跳过）+ 每页 `build_render_blocks` port `blocks.py` 全管线（seed_render_fields→build_block_payloads→(inner_bbox[1],inner_bbox[0]) 排序→`apply_body_payload_pipeline`（font_unify_mode 选阶段）+`unify_annotation_fonts`（mode≠off）+`recover_underfilled_annotation_density`→`mark_adjacent_collision_risk`→按原 index 写回→`emit_render_blocks` 按 payload index 排序）；build_bundle 移除 overlay/dual/auto bail，全五模式 `translated_pages` 全量 color-adapted，overlay/dual 另产出 `overlay_page_specs`（Option→null），`font_unify_mode` port `layout.FONT_UNIFY_MODE`（role_min 默认、off 显式）；`assemble.rs` 增 `overlay_page_specs: Option<Value>`；差分 smoke 全删 `_UNSUPPORTED_MODES`/`_strip_color_adapt_keys`，overlay/dual 增 `_ASSERTED_KEYS`，五模式全量 compared（43 单测 + 五模式 parity 全绿，release 二进制非 stale 是关键） | 完成 | e38b0a49 |

## 子系统对照（已接线 / 休眠 / 未移植）

| Python 子系统 | 对应 Rust | 状态 |
|---|---|---|
| `source/background/stage.py`（build_clean_background_pdf） | `rendering_writer::background`（stage/redaction/detect/image_route/toc/formula_guard/vector_text） | **已接线**：生产走 Rust |
| `source/preparation/hidden_text_strip.py`（build_hidden_text_stripped_pdf_copy） | `rendering_writer::hidden_text`（strip_hidden_text_pages 候选页过滤版） | **已接线（C3-N9）**：fitz 候选页预扫描保持（硬边界），逐页 strip 委托 native；整本 `strip_hidden_text` 保留为 write-diff 参考 |
| `output/typst/`（emitter/compiler/block_renderer） | `rendering_output` | **已接线**：`compiler.py` 经 `_native.emit_typst_source`/`emit_typst_book_overlay_source` 生成源码；typst CLI 编译仍在 Python；单页/dual-book/overlay_book 的 `show_pdf_page` 合成已接 native（B-F，`rendering_writer::overlay`） |
| `document/pdf_ops.py`（save_optimized_pdf 子集化+字节压缩） | `rendering_writer::save::subset_and_clean`（C shim `c/save_clean.c`：mupdf `pdf_subset_fonts` + garbage=4/流压缩） | **已接线**：生产走 `source/_native.py::save_optimized` 全 native（含字体子集化），失败回退纯 fitz |
| `legacy/pdf_compress` | `rendering_writer`（save/strip/compress/extract_pages/overlay） | **休眠**：bridge 已导出，Python 无调用点 |
| `layout/`、`analysis/route/` | `rendering_core` | **未接线**：无 Python 引用，仅差分测试 |
| `analysis/profile/`、`classifier.py` | `rendering_core::profile/classifier` | **已接线（B-A）**：生产走 native 整本文档分析（`build_render_document_analysis`）；per-page `classify_render_page_pdf` 经 bridge 薄函数可达，fitz reference 保留为 `parity_reference`；差分 `differential_page_profile`/`differential_route` 常驻 |
| `source_cleanup/` | `rendering_core::source_cleanup` | **已接线（C3-N1/C3-N10）**：planning 候选装配生产走 bridge（`plan_source_cleanup_native`/`uncovered_unsafe_vector_item_ids_native`），NATIVE=False 回退 Python ctx 参考；红批 executor 的 bbox-text-strip 已接 native（C3-N10，`strip_bbox_text_rects_from_pdf_copy` 经 `source_cleanup/pdf/_native.py` 路由，`skip_form_xobject_pages`/deadline 门禁回退 Python） |
| `pdf_structure_profile/`（sampler） | `rendering_reader`（read_page_cleanup_contexts/form_xobjects/geometry/text_spans 原语 + `_native.py` 装配） | **已接线**：生产走 native，回退 Python |
| `analysis/document`（builder） | `rendering_bridge::build_render_document_analysis`（reader 原语装配） | **已接线**：生产走 `analysis/_native.py`，回退 Python |
| fitz 读取层 | `rendering_reader`（mupdf-rs） | **部分**：pdf_structure_profile / analysis 生产使用 reader 原语；其余仍用 fitz |
| `visual_profile`、`workflow`、`document` | — | **未移植** |
| `policy/`（policy-fields 边界） | `rendering_core::layout::policy_fields` | **已接线（C3-N8）**：`apply_render_pages_policy_fields`/`apply_render_page_policy_fields` 生产走 bridge；其余 policy 逻辑（geometry/models/compat/cover-fallback）仍 Python |

## 生产接线现状

- **C1（骨架 + 委托）**：`backend/rendering_orchestrator/` 提供 `render_rs --spec <spec>`，镜像 `render_only.py` 编排。`delegate.rs` spawn `run_render_delegate.py` 产出 `render.bundle.v1`（prepare + page_specs + visual profile fill map）；`stages/background.rs`→`typst.rs`→`save.rs` 全 native 直调（`build_clean_background_pdf` + `compile_typst_source` + `copy_toc`/`save_optimized`）。差分门禁 `rendering_writer/differential/orchestrator_parity.py` 双二进制 subprocess，已接 `rendering-parity.yml` bridge loop。本地验证：fixture 双 mode 页 facts 全等、像素逐字节相同、体积比 0.94（Rust 更小）。
- **C1·Ext（overlay/dual 接管）**：delegate 对 overlay/dual 产出 `overlay_page_specs`（原 source 页几何 + RenderBlock DTO，尺寸读原始 source，与生产 `read_source_page_sizes` 一致）+ `start_page`/`end_page`；overlay 走 `stages/overlay.rs`（emit overlay source + `book-overlay` 无 `--root` 编译 + `merge_overlay_onto_base` 复用 `rendering_writer::overlay::show_pdf_page` 逐页合成）；dual 走 `stages/dual.rs`（overlay 编译 → merge → `build_dual_doc_pages` 双栏合成 + TOC 复制），`build_dual_doc_pages` 抽到 `rendering_writer::overlay` 与 bridge 共享。`orchestrator_parity` 扩到 typst/typst_visual/overlay/dual 四模式，`smoke_zero_fitz` delegate 探针扩到四模式。
- **C3（生产接线 + 桌面打包）**：`rust_api` `render_only_command`（`entrypoints.rs`）按 `render.render_mode` 默认路由——`should_route_render_rs` 匹配 typst/typst_visual/overlay/dual/auto 全发 `render_rs --spec`（auto 由 delegate 解析为具体模式，非五模式回退 python）；逃逸阀 `RETAINPDF_RENDER_ORCHESTRATOR_RS=1` 强制 native、`RETAINPDF_RENDER_ORCHESTRATOR_OFF=1` 强制 Python。`render_rs_bin`/`render_rs_delegate_script` 进 `RuntimePathsConfig`（`paths.rs::resolve_render_rs_bin`：env `RETAIN_PDF_RENDER_RS_BIN` → 仓库 target → `app/backend/bin/render_rs` → PATH）；`spawn_worker_process` 识别 render_rs 时透传 `RETAIN_PDF_PYTHON_BIN`+`RETAIN_PDF_RENDER_DELEGATE_SCRIPT`（delegate 用 desktop bundled python）；worker 输出契约闭合：`process_contract.rs` 识别 `[render_rs, --spec, ...]` 为 Render contract，render_rs 写 `artifacts/pipeline_summary.json` 并打印 `summary:` 标签（`output pdf`/`summary` 两 artifact 校验齐全）。桌面：`prepare-app.mjs::resolveRenderRsBinary` 拷贝 `bin/render_rs` + manifest 字段，`package.json build:render-rs`，`release-desktop.yml` 每平台构建（linux apt clang/libclang/pkg-config、mac brew pkg-config、win LLVM）。
- native 入口：`build_clean_background_pdf` → `source/background/_native.py`；最终保存 `save_optimized_pdf` → `source/_native.py::save_optimized` 全 native（`doc.tobytes()` → bridge `subset_and_save_optimized_pdf` → `save::subset_and_clean`，C shim 在隔离 mupdf context 里跑 `pdf_subset_fonts` + garbage=4/流压缩，异常经 `mupdf_error_t**` 返回而非 exit；失败回退纯 fitz 子集化+save）。
- 新增 native 入口：`pdf_structure_profile` sampler → `_native.build_pdf_structure_profile`；`analysis/document/builder` → `_native.build_render_document_analysis`；per-page 分类 → `classifier.py::classify_render_page_pdf` → bridge `classify_render_page`（B-A，page_snapshot → `rendering_core::classifier`，回退 fitz reference）；Typst 源码生成 → `output/typst/_native.py::emit_typst_source`/`emit_typst_book_overlay_source`（均回退 Python）；TOC 复制 → `document/_native.py::copy_toc`/`copy_toc_for_page_map`（B-G，返回 doc 换手，回退纯 fitz）。
- 接线现状（B-G 后）：auto / visual_cover / visual_cover_and_remove_text / `text_layer_only` / `text_redaction` 全走 Rust（text_layer_only 四子路由 image_page/cover_only_count/vector_heavy/standard 全量 native）；仅 mock（instrumented）场景回退纯 Python。
- CI 门禁（D2）：`.github/workflows/rendering-parity.yml` 跑 rendering crates 差分 replay（writer+reader，含 form_xobjects）+ 34 个 native 冒烟桥（含 B3 整本 E2E + 像素 parity、`smoke_zero_fitz` 终态判据 1、`smoke_d1_mandate` D1 治理、`orchestrator_parity` 双二进制，CI 装 typst 0.14.2 + cmarker/mitex），锁 native==fitz 页 facts、体积与像素。
- **D1（双实现清零）**：`_routing.py` 的 `ALLOWLIST` 逐项登记保留 Python 双实现的原因——45 项 routed fn 全部为差分参考（`parity_reference`），`collect_page_drawing_rects` 为不可复现分歧硬边界（`hard_boundary`，native rect 在 stroked zigzag 会改红批输出）；mandate 默认 ON，新增未登记 fn 一旦 native 不可用即抛 `NativeMandatoryError`（`RETAIN_PDF_NATIVE_MANDATE=0` 逃逸），`smoke_d1_mandate` 双向扫描保证 allowlist 与 routed 调用面实时一致。
- **D3（边界契约化）**：六条 Python↔Rust JSON 边界（emitter/redaction page_specs、translated_pages、fill_map、precleaned_page_indices、render_bundle）以 `contracts/bridge_shapes.py` 的 TypedDict 为单一 key 声明源；corpus 生成器跑真实生产 producer + `assert_keys_match` 先验，Rust strict gate（deny_unknown_fields 复刻）与宽松 DTO 双解码同验 corpus，Rust 侧不重复声明形状；CI 新增 orchestrator cargo test 与 corpus 再生成 diff 门禁，形状漂移若忘再生成即失败。
- **D4（统一路由与可观测）**：feature-flag 强制关闭与桥缺失在回退原因上区分（`FORCED_OFF` vs `NATIVE_NOT_BUILT`），每回退原因日志更准；`/metrics` 暴露按子系统 hits/fallbacks-by-reason/hit_ratio，`/health` 补 `native_fallbacks`（subsystem→reason→count）维度，native 命中率与回退原因可按子系统观测。
- **D5（目标度量基线）**：四类目标基线登记于 `devtools/d5_baseline.json`——桌面体积（`sizesBytes.total`，max 200MiB）、Python 渲染进程（`render_jobs_total{renderer=python}` 目标 0）、渲染耗时（`render_elapsed_seconds_sum/count` 均值，max 60s）、native 命中率（`native_hit_ratio{subsystem}`，min 0.9）；`d5_baseline.py record` 从 bundle-manifest + /metrics 刷新 observed 参考值，`check` 对照当前观测逐项判定（未观测 PENDING 不阻断、违规退出 1）；实时对照 `d5_baseline.py check --base-url <api>` 运行期操作，CI 以 committed 目标态 sample 自测工具 + `test_d5_baseline.py` 29 用例。
- **终态判据 4（fitz import 可枚举 allowlist）**：`devtools/architecture_checks/fitz_imports.py` 的 `check_fitz_import_allowlist` 常驻 `pipeline.main()`——扫描 `backend/scripts`（devtools/ 豁免），每个 import fitz 的非 devtools 模块必须登记 `FITZ_IMPORT_ALLOWLIST`（100 项，分类 `hard_boundary`/`fallback_reference`/`non_default_write`/`non_render_service` + reason）；stale 条目（已停 import）与未登记新 import 即失败，保证面只能收不能长。100 项按 doc 15 四类：46 `hard_boundary`（words-clip/get_texttrace/pixmap 采样等 mupdf-rs 无等价物 + source_cleanup redaction 写）、46 `fallback_reference`（`_native.py` shim 的 fitz 回退/差分参考）、4 `non_default_write`（dev_overlay/direct_overlay/side_by_side 调试写）、4 `non_render_service`（ocr_provider/translation/entrypoint）。附带清除 5 个死 import（`patch`/`color_sampling`/`text_rects`/`redaction_padding`/`path_removal`），面从 104 → 100。
- 生产渲染流程其余环节：整本 overlay 默认路径（`build_book_typst_pdf` → pikepdf 合并）已零 fitz 调用（B2-Inc5，用调用计数探针验证）；`save_fast_pdf` 仍走 fitz `doc.save`；颜色适配（`apply_adaptive_overlay_colors_batch`）PDF 访问已 native（`sample_page_color_fills`/`extract_page_span_dicts`/`sample_title_visual_colors`），参考实现已清纯几何 `fitz.Rect` 强转（B-D，几何用纯 `Rect`、fitz 消费边界 `to_fitz()`）；单页/dual-book/overlay_book 的 `show_pdf_page` 合成已接 native（B-F：`_native.show_pdf_page_on_doc`/`build_dual_doc_pages`，native 返回新 doc、回退原地 + doc_slot 换手）；`page_overlay.overlay_pages_from_single_pdf` 的逐页 `show_pdf_page` 合成经 `compositor` 注入接 native（source 层不直接 import `output/typst._native`，由 `output/typst/source_page_overlay` wrapper 注入 `show_pdf_page_on_doc`，doc_slot 换手写回，`legacy_pymupdf_overlay_pages` 仅真实 fitz fallback 自增）——覆盖 dual-book 翻译页合成与无 pikepdf 路径的整本 overlay；TOC 复制（`copy_toc`/`copy_toc_for_page_map`）已接 native（B-G：`rendering_writer::background::toc` 全 native 读写，`document/metadata.py` 经 `document/_native.py` shim 返回 doc 换手，回退纯 fitz）。

## Python 依赖评估

- **运行时硬依赖部分下降**：typst/typst_visual/overlay/dual/auto 渲染已全部由 `render_rs` 生产接管（C3·Ext），background→typst→save 段不再 spawn python；但 prepare/page_specs 仍委托 `run_render_delegate.py`（Python 子进程），渲染管线整体仍依赖 Python 运行时。
- fitz/PyMuPDF 仍被 100 个非 devtools 模块引用（终态判据 4 allowlist 可枚举，每项带 reason；`devtools/architecture_checks/fitz_imports.py` 门禁常驻）。
- 仅在 `build_clean_background_pdf` stage 内部，PDF 读写由 mupdf-rs 替换 fitz。
- native `.so` 已构建并装入 `.venv`（Python 3.14），开发环境 `NATIVE=True`。
- 桌面发布版**已接 native（C4）**：`release-desktop.yml` 三平台 maturin build `rendering_bridge` wheel → `pip install --target` 装进 bundled python site-packages，组装期 import check 含 `rendering_bridge`；`prepare-app.mjs` 的 `bundledRenderingBridgePresent`（兼容顶层 `.so/.pyd` 与 `rendering_bridge/` 包目录两种 wheel 布局）写 `renderingBridgeBundled` manifest 字段，`.github/scripts/validate_desktop_bundle.py` 门禁要求为 true；`rendering-parity.yml` 增 `bridge-bundle-smoke`（ubuntu/windows/macos 矩阵：wheel 构建 + 隔离安装 + import）在 tag 发布前拦截 win/mac 构建或加载回归。发布包内 `_native.py` shims 走 `NATIVE=True`（含 save 全 native `subset_and_clean`）。maturin 仍是构建期依赖，不进桌面运行时。

## 分歧台账（reader 原语 vs fitz）

- **text_traces 为空**：mupdf 无 `get_texttrace` 等价物（spans/drawings 无 opacity/type-3 信号）→ native `hidden_text=False`；影响仅限「隐藏文本且 <20 词」页，分类 parity 用 corpus kind 断言兜底。
- **text_intrusion 空白 span**（B-C）：spans/blocks 原语按非空文本契约丢弃纯空白 span，`collect_page_intrusive_display_text_rects` 因此不再 flag 裸空白 span（原始 `get_text("dict")` reference 会）；影响仅「span 全部为空白」的 display 区域，smoke_text_read_bridge 已断言 native==回退。
- **image_rects xref tie**：mupdf 无 xref 关联 bbox，`page_snapshot` 取首个资源 xref，image_rects 聚合全部 placement rects（≤0.01pt 覆盖精确关联）。
- **drawing rect 分歧**：已有 golden 差分记录。
- **native 页索引推导边界**（B2-Inc5 doc=None 分支）：`layout._native.read_source_page_sizes` 会跳过不可读页，与 fitz `0 <= idx < len(doc)` 仅在「范围内但不可读」页有边角分歧；`overlay_pdf_size_mismatches` native 分支假设 overlay 页数==specs 数（fitz 用 `len(overlay_doc)` 实测），编译按 spec 生成、页数恒等，仅防御性检测有差异。
- **颜色适配**：batch 路径（`apply_adaptive_overlay_colors_batch`）PDF 访问已 native（3 个 source native 原语：`sample_page_color_fills`/`extract_page_span_dicts`/`sample_title_visual_colors`），共享决策树 `_apply_adaptive_overlay_colors_with_data` 纯 Python；参考实现（`PageTextColorSampler.build`/`title_text_color_from_visual_components`）仍走 fitz（B-D 后纯几何 `fitz.Rect` 强转已清除，仅剩必需 fitz 消费）。
- **限定为 fallback/非默认策略**：`workflow/direct_overlay.py`、`fill.py`、`source_cleanup/pdf/document.py` —— NATIVE=False 时仍走 fitz，属参考实现。`source_page_overlay`/`overlay_ops`/`overlay_book` 及 `page_overlay.overlay_pages_from_single_pdf`（经 compositor 注入）的 `show_pdf_page` 合成已接 native（B-F）；其余 fitz 逻辑仍属参考实现。
- **source 顶层留 reference（B-E）**：`document_ops.page_word_count`/`_visible_text_traces`/`page_has_editable_text`/`page_is_pseudo_editable_scan` 依赖 `get_texttrace` 与 `get_text("words")`（mupdf-rs 无等价物，硬边界）——`page_is_pseudo_editable_scan` 仍是 strip_hidden_text 的 fitz 预扫描（硬边界；C3-N9 后逐页 strip 走 native `strip_hidden_text_pages`，预扫描语义留在 Python）；`vector_profile.collect_page_drawing_rects` 的 per-drawing zigzag rect 也留 reference（fitz lineart 丢末点，复刻脆弱）。

## 结论

移植覆盖度高、每阶段带 corpus + 差分门禁（现已接 CI），但生产接入度仍偏低：真正跑 Rust 的热点是"背景涂改/红批"stage、最终保存（子集化+字节压缩，全 native）、pdf_structure_profile 采样、Typst 源码生成。C3-N1 已把 source_cleanup/planning 候选装配（`plan_source_cleanup`/`item_ids_with_uncovered_unsafe_vector_overlap`）接 native（bridge + ctx 参考回退）。C3-N2 已把 layout seed 边界（`build_block_payloads`）与 emit 边界（`emit_render_blocks`，inline_content/fit/toc 装配 + bridge + shim 路由）接 native；C3-N3 已把中间 body-pipeline 边界（`apply_body_pipeline` + 整本 `resolve_book_body_font_target`，含 caption/footnote annotation 阶段）接 native；C3-N4 已把碰撞边界（`mark_adjacent_collision_risk`，`blocks.py` 自纯 Python collision 导入改走 `_payload_native`，顺带修 Rust 端口 re-fit 分支缺 `adjacent_collision_risk` 标记的 parity 缺口）；C3-N5 已把 seed 边界（`seed_render_fields`，Value 级直改 + 文本流/行结构 preserve 机制移植，`blocks.py` 的 `build_render_blocks` 首步改走 `_payload_native`），block_seed→seed→body-pipeline→碰撞→emit 整条生产路径不再走 Python（`observe_payload_typography` 仍为进程内 learning，刻意留 Python）。C3-N6 已把 page-specs 装配段（`page_specs._layout_page_spec`）统一接 native（镜像 `blocks.py` 的 `apply_body_pipeline`/`mark_adjacent_collision_risk`/`emit_render_blocks`，原路径缺的 `recover_underfilled_annotation_density` 一并补齐到 C3-N3-blessed 行为），delegate 与 book_renderer 的 page_specs 不再走纯 Python——block_seed→seed→body-pipeline→碰撞→emit 之外，page-specs 装配段也整体 native（title_fit 预算/learning 仍 Python）。C3-N7 已把 prepare 装配段（`prepare_render_payloads_by_page`，group/continuation 文本拆分 + suspicious-OCR 粘连块剔除 + first-line-indent 附着）接 native，`build_render_page_specs` 的 payload 链 prepare→seed→body-pipeline→碰撞→emit 不再走 Python。C3-N8 已把 policy-fields 边界（`apply_render_pages_policy_fields`/`apply_render_page_policy_fields`，typst-fill/默认文字覆盖填充/no-op 三模式把 `_render_policy` 写回 item）接 native（`rendering_core::layout::policy_fields`，`_routing` 增 `policy` 子系统 + allowlist 41→43），`prepare_translated_pages_for_render` 的 prepare→policy 两段不再走 Python。C3-N9 已把 source hidden-text strip 边界（`build_hidden_text_stripped_pdf_copy`：fitz 候选页预扫描保留（硬边界），逐页 strip 委托候选页过滤版 native `strip_hidden_text_pages`）接 native（`rendering_writer::hidden_text` 共享 `strip_hidden_text_on_pages` + bridge 导出 + shim 路由 + allowlist 43→44），预扫描后的逐页 strip 不再走 Python。C3-N10 已把红批 executor 的 bbox-text-strip 边界（`strip_bbox_text_rects_from_pdf_copy`：逐页 strip + Form 递归）接 native（`source_cleanup/pdf/_native.py` shim + bridge `strip_bbox_text_rects` 返回 StripPdfResult 元数据 + `_routing` 增 `source_cleanup` 子系统 + allowlist 44→45），`skip_form_xobject_pages`/deadline 门禁仍走 Python reference。B2 已把整本 overlay 默认路径拉成零 fitz（CI 门禁断言）；颜色适配 batch 已 native，参考实现仅剩必需 fitz 消费（B-D 已清纯几何 `fitz.Rect` 强转）；保存字节压缩已补上 mupdf 侧 `subset_fonts`（C shim 隔离 context + 异常转 `mupdf_error_t**`），不再需要 fitz 子集化，失败才回退纯 fitz。B-F 已把单页/dual-book/overlay_book 的 `show_pdf_page` overlay 合成接 native（逐页回退时不再经 fitz），并补上 `overlay_pages_from_single_pdf` 逐页合成（`compositor` 注入，覆盖 dual-book 翻译页与无 pikepdf 整本路径，`legacy_pymupdf_overlay_pages` 归零，仅真实 fallback 自增）。B-G 已把 TOC 复制（`copy_toc`/`copy_toc_for_page_map`）接 native——`rendering_writer::background::toc` 全 native 读写（含 start/end 范围与 page-map 变体），`document/metadata.py` 的 `get_toc`/`set_toc` 不再走 fitz（回退纯 fitz）。桌面发布版已随 C4 打包 native bridge（三平台 wheel → bundled python，validator + 三 OS 冒烟门禁），`NATIVE=True` 不再仅限开发环境。整体替代程度按代码量算中等，按运行时算偏低；Python 库的整体依赖尚未实质下降。
