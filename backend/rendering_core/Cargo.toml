[package]
name = "rendering_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "rendering_core"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
